# Spring Framework PetClinic v0.8 Boundary-Soundness Run

This directory contains the public-safe v0.8.0 Boundary-Soundness reachability readout.

Inputs:

- v0.4.1 Git-object-byte hash provenance
- v0.5 risk hint gradation
- v0.6 policy mapping
- v0.7 scoped receipt validation

The run checks whether forbidden authority markings are reachable in the bounded public governance path. It does not execute code, mutate source, create patches, create PRs, merge, deploy, claim security remediation, claim product readiness, prove compliance, provide BPMN/DMN engine soundness, or grant runtime authority.
